console.log("Hello");

const multi =(num) =>{}

const nums = [48,22,43]
for(let i = 0 ; nums.length ; i++) {
    const result = multi(nums[i])
    if(result === ??) {
        console.log(`${nums[i]} 테스트 케이이스는 3의 배수`)
    }
}